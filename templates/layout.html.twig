<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="{{  asset("css/app-adaptation.css") }}">
    <link rel="icon" href="{{ asset("img/logo/favicon.png") }}" />
    <title>{% block title %}Adaptation{% endblock %}</title>
</head>
<body>
<div class="container">
    <header>
        <img src="{{ asset("img/logo/logo.png") }}" alt="Logo" width="100px" height="100px">
        {% include('inc/nav.html.twig') %}
    </header>
    <br>
    {# Ici il s'agit de l'ajout de message flash (pour signaler que quelque chose
        c'est bien passé pour non. On le met dans le layout pour être sûr que ça s'affichera
        sur n'importe quelle page
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="flash flash-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}#}
    <div id="page-container">
        <div id="content-wrap">
            {% block main %}{% endblock %}
        </div>
        <footer id="footer">
            &copy;{{ "now" | date("Y") }} - Adaptation<br>
            <a href="https://icons8.com/icon/81967/film">Film icon by Icons8</a>
        </footer>
    </div>
</div>
<script src="../js/main.js"></script>
</body>
</html>